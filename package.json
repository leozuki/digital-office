{
  "name": "digital-office",
  "version": "1.0.0",
  "description": "AI Multi-Agent Workflow Management System",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npm install --prefix backend --no-optional --omit=dev && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
    "start": "node backend/server.js",
    "dev": "npm run dev --prefix backend"
  }
}
